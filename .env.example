# DotVests frontend environment variables
# Copy this file to .env.local and fill in values for local development.
# In production (Vercel), set these in the Vercel project's Environment Variables dashboard.

# Backend API base URL — Render deployment URL for production, localhost for local dev
VITE_API_BASE_URL=https://your-backend.onrender.com
